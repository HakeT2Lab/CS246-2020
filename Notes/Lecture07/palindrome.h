#include <iostream>
#include <string>
#include <sstream>

std::string palindrome(){
    //if string is equal to the stack of the string return true

    Stack s;
    for(int i = 0; exp[i] != '\0'; i++){
        s.Push(exp[i]);
    }
    int i= 0;
    while(i.IsEmpty() && exp[i] != '\0'){
        if(s.Top() != exp[i]){

        }
    }
}